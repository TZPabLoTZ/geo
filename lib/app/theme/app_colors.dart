import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primary = Color(0xFF3AD3D3);

  static const black = Color(0xFF000000);
  static const white = Color(0xFFFFFFFF);

  static const secondary = Color(0xFF3E3E3E);
  static const text = Color(0xFF080A24);

  static const backgroundBlack = Color(0xFF000000);
  static const backgroundLight = Color(0xFFF5F5F5);

  static final border = Color(0xFF000000).withValues(alpha: 0.1);
}
